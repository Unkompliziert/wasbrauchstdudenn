# Copy to .env.local and fill in. Never commit real secrets.
# Not used yet in M1 (blank page). Added for the M3 conversation endpoint,
# which calls the AI provider server-side so the key never reaches the browser.
ANTHROPIC_API_KEY=
